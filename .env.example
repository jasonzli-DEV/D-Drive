# D-Drive Environment Configuration
# Most settings are configured via the web setup wizard at first run

# Database (auto-generated by install script)
POSTGRES_PASSWORD=your_secure_password_here

# JWT Secret (auto-generated by install script)
JWT_SECRET=your_jwt_secret_here

# Frontend Port (default: 80)
# Use a different port if running behind a reverse proxy (e.g., 3000)
FRONTEND_PORT=80

# URLs
FRONTEND_URL=http://localhost
VITE_API_URL=http://localhost:5000

# Discord Configuration (set via web setup wizard)
# These will be configured when you first visit the application
# DISCORD_CLIENT_ID=
# DISCORD_CLIENT_SECRET=
# DISCORD_BOT_TOKEN=
# DISCORD_GUILD_ID=
# DISCORD_CHANNEL_ID=
# VITE_DISCORD_CLIENT_ID=
